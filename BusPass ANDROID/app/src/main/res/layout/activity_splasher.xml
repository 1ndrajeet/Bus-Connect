<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/material_dynamic_primary10">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/bus"
        android:contentDescription="Bus Logo"
        android:layout_marginBottom="24dp" />

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BusConnect"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/material_dynamic_secondary100"
        android:layout_marginBottom="12dp" />

    <TextView
        android:id="@+id/tagline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Book with ease, travel with confidence!"
        android:textSize="18sp"
        android:textColor="@color/material_dynamic_secondary80" />

</LinearLayout>
